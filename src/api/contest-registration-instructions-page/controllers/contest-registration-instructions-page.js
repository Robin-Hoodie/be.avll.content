'use strict';

/**
 * contest-registration-instructions-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contest-registration-instructions-page.contest-registration-instructions-page');
