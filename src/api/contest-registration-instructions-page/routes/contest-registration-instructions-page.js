'use strict';

/**
 * contest-registration-instructions-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contest-registration-instructions-page.contest-registration-instructions-page');
