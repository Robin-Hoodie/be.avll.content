'use strict';

/**
 * contest-registration-instructions-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contest-registration-instructions-page.contest-registration-instructions-page');
